# 方案三：集中 block_ops 宿主侧报文准备 — 执行计划

## 前置

- 依赖方案一稳定（方块预检与审批复用一的工具生命周期，先统一可减少重复）。须在方案一验收后开始。
- 工作分支：`refactor/block-ops-host-message-prep`。

## 实施顺序

1. **读当前代码**：核对 `tools_impl.py`、`target.py`、`__init__.py`、`bridge.py`、`preflight_cache.py` 实际结构（报告行号已偏移）。
2. **建立特征测试**：为报告"建议验证的行为"（目标归一化、数量限制、字节预算、预检/冻结/审批恢复、Addon 结果、三工具一致错误格式）补测试作为基线。
3. **上移限制/载荷逻辑**：将限制注入与字节预算从 `target.py` 函数内反向导入中解脱，上移到共享深模块；`target.py` 只做纯目标归一化。
4. **集中宿主侧报文准备**：把目标归一化、限制、预算、Addon 报文、预检计划、结果裁剪收敛到深模块；三个工具只提交意图和参数。
5. **收窄 `__init__.py` 出口**：只保留稳定入口（桥、能力缓存、预检缓存、三个工具），移除内部实现重导出。
6. **统一错误格式**：确认三个工具对同类错误输出一致结果格式。
7. **质量检查**：`pytest -q tests/test_block_ops.py` 及工具相关测试，并跑全量 `pytest -q`。

## 验证命令

```bash
pytest -q tests/test_block_ops.py
pytest -q   # 全量，确认无新增失败
```

## 评审门

- 代码评审：确认 `target.py` 不再反向导入 `tools_impl`；`__init__.py` 出口已收窄；三工具错误结果一致。
- 通过 `trellis-check` 复核，再合入 `dev`。

## 回滚点

- 改动集中在 `block_ops/` 包。若验收门不满足（仍存在反向依赖 / 错误结果不一致），回退到合入前提交。

## 完成定义

- `target.py` 不再反向导入 `tools_impl`。
- `block_ops/__init__.py` 出口收窄。
- 三工具对同类错误结果一致。
- 全量 pytest 无新增失败。